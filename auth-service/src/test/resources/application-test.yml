spring:
  jpa:
    hibernate:
      ddl-auto: validate      # Flyway creates schema → Hibernate validates
    show-sql: true
  flyway:
    enabled: true             # run migrations against testcontainers postgres
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

application:
  security:
    jwt:
      secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
      expiration: 86400000
      refresh-token:
        expiration: 604800000
